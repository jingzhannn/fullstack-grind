import '../components/loginCard.css'

const SignUp = () => {
  return (
    <main className="login-page">
      <section className="login-card" aria-label="Sign up page placeholder">
        <h1 className="login-title">Sign Up</h1>
      </section>
    </main>
  )
}

export default SignUp
